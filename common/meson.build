common_src = [
  'command.c',
  ]

common_dep = [

  ]

common_a = static_library(
  'common',
  common_src,
  dependencies: common_dep,
  include_directories: include_directories('..'),
  name_prefix: '',
  )
